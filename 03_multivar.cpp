#include<iostream>

using namespace std;

int main(){
    // int age=24, marks=89, rollno=17; // decleration of multiple variables in single statement

	// cout << "Age of the student is: "<<age<<endl;
    // cout << "Marks of the student is: "<<marks<<"%."<<endl;
    // cout << "Rollno of the student is: "<<rollno;

    int a,b,c;
    a = b = c = 50;

    cout << "Value of a: " << a << endl;
    cout << "Value of b: " << b << endl;
    cout << "Value of c: " << c << endl;

    // names can contain letters, digits and underscores
    // names must begin with a letter or an underscore
    // names must not contain special characters like !,#,% etc.
    // names are case sensitive
    // reserved words cannot be used as names such as (int). 

    return 0;
}